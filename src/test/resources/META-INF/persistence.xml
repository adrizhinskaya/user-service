<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="user_service_test" transaction-type="RESOURCE_LOCAL"> <!-- ВОТ ЗДЕСЬ ДОЛЖНО БЫТЬ user_service_test -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>org.example.model.User</class> <!-- Убедитесь, что это правильный пакет! -->

        <properties>
            <!-- Эти значения будут подставлены Testcontainers через системные свойства -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="${tc.jdbc.url}"/>
            <property name="jakarta.persistence.jdbc.user" value="${tc.jdbc.username}"/>
            <property name="jakarta.persistence.jdbc.password" value="${tc.jdbc.password}"/>

            <!-- Настройки Hibernate. Убрали hibernate.dialect как рекомендовалось. -->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="false"/>
        </properties>
    </persistence-unit>
</persistence>
